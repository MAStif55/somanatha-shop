<USER_REQUEST>
я еще поспрашивал у другого ии агента, в чем могут быть проблемы. вот что он написал:
"Вот конкретный чеклист и технические нюансы для проверки SSE-чата. Тестировать нужно в два этапа: сначала локально, а потом обязательно на тестовом контуре (или проде) за Nginx, потому что поведение SSE в этих средах кардинально отличается.

1. Тест на «залипание» сообщений (Проверка буферизации Nginx)
Локально (localhost) сообщения будут летать мгновенно. Главная засада начнется, когда код уйдет на сервер под Nginx.

Как проверять: Открой чат клиента и чат админа. Напиши короткое сообщение («Привет»). Оно должно появиться на втором экране мгновенно (за доли секунды).

Симптом проблемы: Если сообщение появляется только после того, как ты отправишь еще 3–4 длинных сообщения, или спустя фиксированное время (например, секунд 30, когда срабатывает пинг) — значит, Nginx буферизирует поток. Он ждет, пока наберется условные 4 Кб данных, прежде чем отдать их клиенту.

Что делать: В конфиг Nginx для роута чата (/api/app/orders/[id]/chat/route или общего /api/) добавь директивы:

Nginx
proxy_buffering off;
proxy_cache off;
chunked_transfer_encoding on;
2. Главная архитектурная мина: Проверка режима PM2 (Cluster 
<truncated 4364 bytes>
чика браузера, найди запрос чата. Соединение не должно прерываться или постоянно перезапускаться (статус должен оставаться 200 OK или Pending, а во вкладке EventStream должны капать пустые строки или комментарии-пинги каждые 30 секунд).

5. Стресс-тест вкладок
Кейс: Админ открыл три разные вкладки с заказами в админке, а клиент открыл чат на компе и на телефоне одновременно.

Что проверить: Сообщения должны дублироваться во все открытые окна этого конкретного заказа. При этом отправка сообщения из одной вкладки админа не должна вызывать дублирование этого же сообщения на его других вкладках (проверь корректность рендеринга массивов на фронте, чтобы key у элементов списка сообщений был уникальным, например, на основе ID сообщения из MongoDB, а не индекса массива).

Запускай тесты в таком порядке. Если на первом этапе (мгновенная доставка на проде) будут затыки — сразу смотри конфиг Nginx."

что думаешь?
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-06-08T05:09:25-04:00.
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from Gemini 3.5 Flash (High) to Gemini 3.1 Pro (High). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>